Place the ViPDE package source here (not tracked by git). To obtain the source code, follow the access instructions in the repository [README](../README.md).

Expected layout:

- `models/` — ViPDE model (ViPDE class, blocks)
- `utils/` — preprocessing, device selection, TTA, visualization
